# AnimalClassificartion
Classify and Identify Animal uses MobileNetV2, a deep learning convolutional neural network (CNN) model developed by Google, trained on ImageNet—a large dataset for image recognition.
